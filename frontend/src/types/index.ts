export interface IFeatures {
    icon: React.ReactNode;
    title: string;
    desc: string;
}

export interface IStatistics {
    label: string;
    value: string;
    growth: string;
    pct: string;
}
